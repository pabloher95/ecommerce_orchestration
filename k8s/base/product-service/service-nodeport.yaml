apiVersion: v1
kind: Service
metadata:
  name: ecommerce-product-service-nodeport
spec:
  type: NodePort
  selector:
    app: ecommerce-product-service
  ports:
    - port: 3001
      nodePort: 30001
      targetPort: 3001
      name: http