apiVersion: v1
kind: Service
metadata:
  name: ecommerce-order-service-nodeport
spec:
  type: NodePort
  selector:
    app: ecommerce-order-service
  ports:
    - port: 3002
      targetPort: 3002
      nodePort: 30002
      name: http