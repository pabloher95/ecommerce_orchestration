apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend-nodeport
spec:
  type: NodePort
  selector:
    app: ecommerce-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
      name: http